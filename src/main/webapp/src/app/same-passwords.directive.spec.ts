import { SamePasswordsDirective } from './same-passwords.directive';

describe('SamePasswordsDirective', () => {
  it('should create an instance', () => {
    const directive = new SamePasswordsDirective();
    expect(directive).toBeTruthy();
  });
});
